# 🔐 คู่มือระบบ Password สำหรับผู้ป่วย

## ✨ ฟีเจอร์ที่เพิ่มเข้ามา

### 1. QR Code สำหรับผู้ป่วย
- ✅ Admin สามารถดู QR Code ของผู้ป่วยแต่ละคนได้
- ✅ สามารถบันทึก QR Code เป็นรูปภาพ (PNG)
- ✅ QR Code จะลิงก์ไปยัง Patient Portal

### 2. ระบบ Password สำหรับผู้ป่วย
- ✅ ผู้ป่วยสามารถตั้ง password ของตัวเองได้
- ✅ ผู้ป่วยต้อง login ก่อนเข้าดูข้อมูล
- ✅ Admin สามารถปลดล็อกรหัสผ่านผู้ป่วยได้

---

## 📋 ขั้นตอนการใช้งาน

### สำหรับ Admin

#### 1. ดู QR Code และบันทึกเป็นรูปภาพ

1. ไปที่หน้า **Admin → จัดการผู้ป่วย**
2. คลิกปุ่ม **QR Code** (ไอคอน QR) ที่แถวของผู้ป่วย
3. จะแสดง Modal พร้อม QR Code
4. คลิก **"บันทึกเป็นรูปภาพ"** เพื่อดาวน์โหลด
5. ส่ง QR Code ให้ผู้ป่วย (SMS/Email/Line/พิมพ์)

#### 2. ปลดล็อกรหัสผ่านผู้ป่วย

1. ไปที่หน้า **Admin → จัดการผู้ป่วย**
2. คลิกปุ่ม **Key** (ไอคอนกุญแจ) ที่แถวของผู้ป่วย
3. กรอกรหัสผ่านใหม่ (อย่างน้อย 6 ตัวอักษร)
4. คลิก **"ปลดล็อกรหัสผ่าน"**
5. ส่งรหัสผ่านใหม่ให้ผู้ป่วย

---

### สำหรับผู้ป่วย

#### 1. ตั้งรหัสผ่านครั้งแรก

1. ผู้ป่วยได้รับ Link หรือ QR Code จาก Admin
2. คลิก Link หรือ Scan QR Code
3. ระบบจะนำไปหน้า **ตั้งรหัสผ่าน** อัตโนมัติ
4. กรอกรหัสผ่านใหม่ (อย่างน้อย 6 ตัวอักษร)
5. ยืนยันรหัสผ่านอีกครั้ง
6. คลิก **"ตั้งรหัสผ่าน"**
7. ระบบจะนำไปหน้า Login

#### 2. Login เข้าดูข้อมูล

1. ไปที่ URL: `https://yourdomain.com/th/patient/[รหัสผู้ป่วย]`
2. ระบบจะนำไปหน้า Login อัตโนมัติ
3. กรอกรหัสผ่าน
4. คลิก **"เข้าสู่ระบบ"**
5. เข้าดูข้อมูลได้

#### 3. ถ้าลืมรหัสผ่าน

1. ติดต่อ Admin/แพทย์
2. Admin จะปลดล็อกรหัสผ่านให้
3. Admin จะส่งรหัสผ่านใหม่ให้ผู้ป่วย
4. ผู้ป่วยใช้รหัสผ่านใหม่ login ได้

---

## 🔄 Flow การทำงาน

### Flow สำหรับผู้ป่วยใหม่

```
1. Admin สร้างผู้ป่วยในระบบ
   ↓
2. Admin คลิกดู QR Code → บันทึกเป็นรูปภาพ
   ↓
3. ส่ง QR Code ให้ผู้ป่วย
   ↓
4. ผู้ป่วย Scan QR Code หรือคลิก Link
   ↓
5. ระบบตรวจสอบว่าไม่มี password → นำไปหน้า "ตั้งรหัสผ่าน"
   ↓
6. ผู้ป่วยตั้งรหัสผ่าน
   ↓
7. ระบบนำไปหน้า Login
   ↓
8. ผู้ป่วย Login ด้วยรหัสผ่านที่ตั้งไว้
   ↓
9. เข้าดูข้อมูลได้
```

### Flow สำหรับผู้ป่วยเดิม

```
1. ผู้ป่วยเข้าที่ URL: /th/patient/[รหัสผู้ป่วย]
   ↓
2. ระบบตรวจสอบ session
   ↓
3. ถ้ายัง login อยู่ → แสดงข้อมูล
   ↓
4. ถ้ายังไม่ login → นำไปหน้า Login
   ↓
5. ผู้ป่วยกรอกรหัสผ่าน
   ↓
6. Login สำเร็จ → แสดงข้อมูล
```

### Flow เมื่อลืมรหัสผ่าน

```
1. ผู้ป่วยติดต่อ Admin
   ↓
2. Admin ไปที่ Admin → จัดการผู้ป่วย
   ↓
3. คลิกปุ่ม Key (ปลดล็อกรหัสผ่าน)
   ↓
4. ตั้งรหัสผ่านใหม่
   ↓
5. ส่งรหัสผ่านใหม่ให้ผู้ป่วย
   ↓
6. ผู้ป่วยใช้รหัสผ่านใหม่ Login
```

---

## 🔒 ความปลอดภัย

### Session Management
- Session เก็บใน localStorage
- Session หมดอายุใน 7 วัน
- ต้อง login ใหม่เมื่อ session หมดอายุ

### Password Security
- Password ถูก hash ด้วย bcrypt (10 rounds)
- Password ต้องมีอย่างน้อย 6 ตัวอักษร
- Admin สามารถ reset password ได้

---

## 📝 Database Migration

### รัน Migration

```sql
-- รัน migration เพื่อเพิ่ม password fields
-- ไฟล์: supabase/migrations/002_add_patient_password.sql
```

### Fields ที่เพิ่มเข้ามา

- `password_hash` - รหัสผ่านที่ hash แล้ว
- `password_reset_token` - Token สำหรับ reset password (สำหรับอนาคต)
- `password_reset_expires` - วันหมดอายุของ token

---

## 🎯 API Endpoints

### `/api/patient/login`
- **Method**: POST
- **Body**: `{ patient_id, password }`
- **Response**: `{ success, error? }`

### `/api/patient/set-password`
- **Method**: POST
- **Body**: `{ patient_id, password }`
- **Response**: `{ success, error? }`

### `/api/patient/reset-password`
- **Method**: POST
- **Body**: `{ patient_id, new_password }`
- **Response**: `{ success, error? }`
- **Note**: ต้องเป็น Admin เท่านั้น

---

## ✅ Checklist

### สำหรับ Admin
- [ ] รัน migration `002_add_patient_password.sql`
- [ ] ทดสอบดู QR Code และบันทึกเป็นรูปภาพ
- [ ] ทดสอบปลดล็อกรหัสผ่านผู้ป่วย
- [ ] ส่ง QR Code ให้ผู้ป่วยทดสอบ

### สำหรับผู้ป่วย
- [ ] ตั้งรหัสผ่านครั้งแรก
- [ ] Login เข้าดูข้อมูล
- [ ] ทดสอบลืมรหัสผ่าน (ติดต่อ Admin)

---

## 🆘 Troubleshooting

### QR Code ไม่แสดง
- ตรวจสอบว่าได้ติดตั้ง `qrcode.react` แล้ว
- ตรวจสอบ console สำหรับ errors

### Login ไม่ได้
- ตรวจสอบว่าได้ตั้งรหัสผ่านแล้วหรือยัง
- ตรวจสอบว่ารหัสผ่านถูกต้องหรือไม่
- ลองให้ Admin reset password

### ไม่สามารถบันทึก QR Code เป็นรูปภาพ
- ตรวจสอบว่าได้ติดตั้ง `html2canvas` แล้ว
- ตรวจสอบ browser permissions

---

## 📚 Related Files

- `components/admin/PatientQRCode.tsx` - QR Code Component
- `app/[locale]/patient/[patientId]/login/page.tsx` - Login Page
- `app/[locale]/patient/[patientId]/set-password/page.tsx` - Set Password Page
- `app/api/patient/login/route.ts` - Login API
- `app/api/patient/set-password/route.ts` - Set Password API
- `app/api/patient/reset-password/route.ts` - Reset Password API
- `lib/patient-auth.ts` - Patient Auth Helpers
- `supabase/migrations/002_add_patient_password.sql` - Migration



