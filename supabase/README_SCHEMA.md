# Database Schema Guide

## 📁 ไฟล์ Schema ที่มี

### 1. `schema_complete.sql` ⭐ **แนะนำสำหรับการย้าย database**
- **ใช้เมื่อ**: สร้าง database ใหม่ทั้งหมด, ย้าย database, reset database
- **รวมทุกอย่าง**: Tables, Indexes, Triggers, RLS, Policies, Default Data
- **พร้อมใช้งาน**: รันครั้งเดียวได้เลย

### 2. `schema.sql`
- **ใช้เมื่อ**: สร้าง database ใหม่ครั้งแรก
- **รวม**: Tables, Indexes, Triggers, RLS, Policies, Default Data

### 3. `migration_safe.sql`
- **ใช้เมื่อ**: มี database อยู่แล้ว และต้องการเพิ่ม features ใหม่
- **ปลอดภัย**: ไม่มี DROP commands ที่ Supabase จะเตือน

### 4. `migration_add_hospital_id.sql`
- **ใช้เมื่อ**: มี database อยู่แล้ว และต้องการเพิ่ม hospital_id column
- **มี warning**: Supabase อาจเตือนเพราะมี DROP commands

## 🚀 การใช้งาน

### สถานการณ์ที่ 1: สร้าง Database ใหม่ทั้งหมด

```bash
# ใช้ไฟล์นี้
supabase/schema_complete.sql
```

**ขั้นตอน:**
1. เปิด Supabase Dashboard → SQL Editor
2. คัดลอกเนื้อหาทั้งหมดจาก `schema_complete.sql`
3. วางและคลิก **Run**
4. เสร็จสิ้น! ✅

### สถานการณ์ที่ 2: ย้าย Database ไป Supabase ใหม่

```bash
# ใช้ไฟล์นี้
supabase/schema_complete.sql
```

**ขั้นตอน:**
1. Backup ข้อมูลเดิม (ถ้ามี)
2. สร้าง Supabase project ใหม่
3. รัน `schema_complete.sql` ใน project ใหม่
4. Import ข้อมูลเดิม (ถ้ามี)

### สถานการณ์ที่ 3: มี Database อยู่แล้ว และต้องการเพิ่ม Features

```bash
# ใช้ไฟล์นี้
supabase/migration_safe.sql
```

**ขั้นตอน:**
1. เปิด Supabase Dashboard → SQL Editor
2. คัดลอกเนื้อหาทั้งหมดจาก `migration_safe.sql`
3. วางและคลิก **Run**
4. ไม่มี warning ✅

## 📊 โครงสร้าง Database

### Core Tables
- `hospitals` - โรงพยาบาล
- `patients` - ข้อมูลผู้ป่วย
- `tki_records` - ประวัติการใช้ยา TKI
- `test_results` - ผลการตรวจ
- `alerts` - การแจ้งเตือน

### Admin Tables
- `admins` - ผู้ดูแลระบบ
- `guidelines` - แนวทางปฏิบัติ (admin จัดการได้)
- `protocols` - Protocol ต่างๆ (admin แก้ไขได้)

## 🔐 สร้าง Admin User

หลังจากรัน schema แล้ว:

### วิธีที่ 1: ใช้ API (แนะนำ)
```
GET http://localhost:3001/api/admin/init
```

### วิธีที่ 2: ใช้ SQL
```sql
-- ต้อง hash password 'admin123' ด้วย bcrypt ก่อน
-- หรือใช้ API endpoint จะสะดวกกว่า
```

## ✅ ตรวจสอบ Schema

รัน queries เหล่านี้เพื่อตรวจสอบ:

```sql
-- ตรวจสอบว่ามีตารางทั้งหมด
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- ตรวจสอบ hospital_id column
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'patients' AND column_name = 'hospital_id';

-- นับจำนวน records
SELECT 
  'hospitals' as table_name, COUNT(*) as count FROM hospitals
UNION ALL
SELECT 'admins', COUNT(*) FROM admins
UNION ALL
SELECT 'patients', COUNT(*) FROM patients
UNION ALL
SELECT 'guidelines', COUNT(*) FROM guidelines
UNION ALL
SELECT 'protocols', COUNT(*) FROM protocols;
```

## ⚠️ คำเตือน

1. **Backup ก่อน**: หากมีข้อมูลสำคัญ ควร backup ก่อนรัน schema
2. **Production**: ใน production ควรใช้ migration scripts แทนการ drop/create
3. **RLS Policies**: ปัจจุบันเปิดให้ทุกคนเข้าถึงได้ ควรปรับตามความต้องการ

## 📝 Notes

- Schema นี้ใช้ UUID สำหรับ primary keys
- Foreign keys มี ON DELETE CASCADE/SET NULL ตามความเหมาะสม
- มี indexes สำหรับ performance
- มี triggers สำหรับ updated_at columns
- RLS เปิดอยู่แต่ policies อนุญาตทุกคน (ควรปรับใน production)



