# Migration Guide - เวอร์ชันปลอดภัย (Safe Version)

## ⚠️ คำเตือนจาก Supabase

หาก Supabase แสดง warning เกี่ยวกับ "destructive operation" ให้ใช้ไฟล์ **`migration_safe.sql`** แทน

## 🔒 ความแตกต่างระหว่างเวอร์ชัน

### เวอร์ชันเดิม (`migration_add_hospital_id.sql`)
- ใช้ `DROP TRIGGER IF EXISTS` และ `DROP POLICY IF EXISTS`
- Supabase อาจเตือนว่าเป็น destructive operation
- **ปลอดภัย** แต่ Supabase จะเตือน

### เวอร์ชันปลอดภัย (`migration_safe.sql`) ⭐ แนะนำ
- ตรวจสอบก่อนสร้าง trigger/policy
- ไม่ใช้ `DROP` command
- **ปลอดภัย 100%** และ Supabase จะไม่เตือน

## 📋 ขั้นตอนการ Migration

### วิธีที่ 1: ใช้ Safe Migration (แนะนำ)

1. เปิด Supabase Dashboard → SQL Editor
2. คัดลอกเนื้อหาทั้งหมดจากไฟล์ **`supabase/migration_safe.sql`**
3. วางใน SQL Editor
4. คลิก **Run** (จะไม่มี warning)

### วิธีที่ 2: Migration แบบทีละขั้นตอน

หากต้องการทำทีละขั้นตอน:

#### ขั้นตอนที่ 1: เพิ่ม hospital_id column
```sql
-- เพิ่ม hospital_id column
ALTER TABLE patients 
ADD COLUMN hospital_id UUID REFERENCES hospitals(id) ON DELETE SET NULL;

-- สร้าง index
CREATE INDEX IF NOT EXISTS idx_patients_hospital_id ON patients(hospital_id);
```

#### ขั้นตอนที่ 2: สร้างตาราง hospitals
```sql
CREATE TABLE IF NOT EXISTS hospitals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name VARCHAR(255) NOT NULL UNIQUE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

#### ขั้นตอนที่ 3: สร้างตารางอื่นๆ
รันส่วนที่เหลือจาก `migration_safe.sql`

## ✅ ตรวจสอบผลลัพธ์

หลังจาก migration สำเร็จ:

```sql
-- ตรวจสอบว่า column hospital_id มีอยู่แล้ว
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'patients' AND column_name = 'hospital_id';

-- ตรวจสอบตาราง hospitals
SELECT * FROM hospitals LIMIT 5;

-- ตรวจสอบตาราง admins
SELECT * FROM admins;
```

## 🔐 สร้าง Admin User

หลังจาก migration สำเร็จ:

### วิธีที่ 1: ใช้ API (แนะนำ)
เปิดเบราว์เซอร์: `http://localhost:3001/api/admin/init`

### วิธีที่ 2: ใช้ SQL (ต้อง hash password)
```sql
-- ใช้ bcrypt hash สำหรับ 'admin123'
-- หรือใช้ API endpoint จะสะดวกกว่า
```

## ❓ FAQ

### Q: ทำไม Supabase ถึงเตือน?
A: เพราะ script เดิมใช้ `DROP` command ซึ่ง Supabase ถือว่าเป็น destructive operation แต่จริงๆ แล้วปลอดภัยเพราะมี `IF EXISTS`

### Q: ควรใช้เวอร์ชันไหน?
A: ใช้ **`migration_safe.sql`** จะปลอดภัยและไม่มี warning

### Q: ข้อมูลเดิมจะหายไหม?
A: **ไม่หาย** script นี้จะเพิ่ม column และสร้างตารางใหม่เท่านั้น ไม่ลบข้อมูลเดิม

### Q: ถ้า column มีอยู่แล้วจะเกิดอะไรขึ้น?
A: Script จะตรวจสอบและข้ามไป ไม่เกิด error

## 🆘 Troubleshooting

### Error: relation "hospitals" does not exist
- รัน migration script อีกครั้ง หรือสร้างตาราง hospitals ก่อน

### Error: column already exists
- ไม่เป็นปัญหา แสดงว่า column มีอยู่แล้ว

### Error: duplicate key value violates unique constraint
- แสดงว่ามีข้อมูลอยู่แล้ว (เช่น admin user) ไม่เป็นปัญหา



