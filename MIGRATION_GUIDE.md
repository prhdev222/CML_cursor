# 📋 คู่มือการรัน Migration สำหรับ Research Papers และ Patient Education

## ⚠️ สำคัญ: ต้องรัน Migration ก่อนใช้งาน

หากคุณเห็น error เกี่ยวกับ `research_papers` หรือ `patient_education` table ไม่พบ แสดงว่าคุณยังไม่ได้รัน migration SQL

## 🚀 ขั้นตอนการรัน Migration

### 1. เปิด Supabase Dashboard

1. ไปที่ https://supabase.com
2. Login เข้าสู่ระบบ
3. เลือกโปรเจกต์ของคุณ

### 2. เปิด SQL Editor

1. คลิก **SQL Editor** ในเมนูซ้าย
2. คลิก **New query**

### 3. รัน Migration SQL

1. เปิดไฟล์ `supabase/migrations/004_add_research_and_education.sql` ในโปรเจกต์
2. คัดลอก SQL ทั้งหมด
3. วางใน SQL Editor
4. คลิก **Run** (หรือกด `Cmd+Enter` / `Ctrl+Enter`)

### 4. ตรวจสอบผลลัพธ์

คุณควรเห็นข้อความ:
```
Success. No rows returned
```

### 5. ตรวจสอบว่า Tables ถูกสร้างแล้ว

1. ไปที่ **Table Editor** ในเมนูซ้าย
2. ตรวจสอบว่ามี tables ต่อไปนี้:
   - ✅ `research_papers`
   - ✅ `patient_education`

## 📝 เนื้อหาใน Migration

Migration นี้จะสร้าง:

### 1. Table `research_papers`
- สำหรับเก็บข้อมูลงานวิจัย
- Fields: `id`, `title_th`, `authors_th`, `journal_th`, `year`, `description_th`, `url`, `is_active`, `sort_order`

### 2. Table `patient_education`
- สำหรับเก็บเนื้อหาให้ความรู้ผู้ป่วย
- Fields: `id`, `category`, `title_th`, `content_th`, `icon`, `is_active`, `sort_order`
- Categories: `disease_info`, `self_care`, `medication`

### 3. Indexes และ Triggers
- Indexes สำหรับการค้นหาที่เร็วขึ้น
- Triggers สำหรับอัพเดท `updated_at` อัตโนมัติ

### 4. Row Level Security (RLS)
- Policies สำหรับการเข้าถึงข้อมูล

## 🔧 แก้ไขปัญหา

### Error: "relation does not exist"

**สาเหตุ**: Table ยังไม่ได้ถูกสร้าง

**วิธีแก้**:
1. ตรวจสอบว่าได้รัน migration SQL แล้ว
2. ตรวจสอบว่าไม่มี syntax error ใน SQL
3. รัน migration อีกครั้ง

### Error: "permission denied"

**สาเหตุ**: RLS policy ไม่ยอมให้เข้าถึง

**วิธีแก้**:
1. ตรวจสอบว่า migration สร้าง policies แล้ว
2. หรือรัน SQL นี้เพื่อตรวจสอบ:
   ```sql
   SELECT * FROM pg_policies WHERE tablename = 'research_papers';
   ```

### Error: "duplicate key value"

**สาเหตุ**: Table มีอยู่แล้ว

**วิธีแก้**:
- ไม่เป็นไร migration ใช้ `CREATE TABLE IF NOT EXISTS` ดังนั้นจะไม่ error

## ✅ หลังรัน Migration แล้ว

1. รีสตาร์ท development server:
   ```bash
   npm run dev
   ```

2. ทดสอบ:
   - ไปที่ `/admin/research` เพื่อเพิ่มงานวิจัย
   - ไปที่ `/admin/patient-education` เพื่อเพิ่มเนื้อหาให้ความรู้
   - ไปที่ `/research` เพื่อดูงานวิจัย
   - ไปที่ `/patient/[patientId]/education` เพื่อดูเนื้อหาให้ความรู้

## 📚 ข้อมูลเพิ่มเติม

- Migration file: `supabase/migrations/004_add_research_and_education.sql`
- Schema file: `supabase/schema.sql` (รวม migration นี้แล้ว)

